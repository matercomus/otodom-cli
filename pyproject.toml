[project]
name = "otodom-cli"
version = "0.1.0"
description = "Agent-friendly CLI scraper for otodom.pl rental/sale listings"
requires-python = ">=3.14"
dependencies = [
    "beautifulsoup4>=4.12",
    "requests>=2.31",
]
